import React from 'react';

function TodoItem({ data }) {
  // console.log(data);
  return (
  // {
  // data.map((todo) => {

  // return (
    <li>{ data }</li>
  // )
  // })
    // }
  );
}

export default TodoItem;
